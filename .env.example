# Kopieren nach .env und ausfüllen. .env selbst wird NICHT committed (siehe .gitignore).
# Nur nötig, wenn execution.broker in config/config.yaml auf alpaca_paper oder
# alpaca_live gesetzt wird - für den Default (paper) werden keine Keys benötigt.
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
